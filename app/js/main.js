$(function(){
  $('.top-slider__items').slick({
    dots: true,
    arrows: false,
    fade: true,
    autoplay: true,
    autoplaySpeed: 3000
  });

  var mixer = mixitup('.product__items');

  var mixer = mixitup('.design__items');
});